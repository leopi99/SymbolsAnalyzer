name: symbols_analyzer
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  
  fluent_ui: ^3.10.0
  macos_ui: ^0.12.2+2

  rxdart: ^0.27.3

  process_run: ^0.12.3+1

  path_provider: ^2.0.9
  file_picker: ^4.5.1

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true